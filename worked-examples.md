# Worked Examples

SKILL.md의 6단계 파이프라인과 매핑표를, 이진 탐색 외의 문제에 적용한 예제들. 각 예제는 같은 순서(말 → 손풀이 → 암묵지 → 상태→변수 → 흐름 → 코드+트레이싱)를 따른다.

---

## 예제 1: 괄호 짝 맞추기 → 스택(stack)

**문제:** `(`, `)`, `{`, `}`, `[`, `]` 로 된 문자열이 올바르게 닫히는지 판별하라. 예: `"({[]})"` → True, `"(]"` → False.

**1) 말 + 예제:** "여는 괄호는 나중에 같은 종류로 닫혀야 한다." 예제 `"({)}"`로 손풀이.

**2) 손풀이 관찰:** 여는 괄호를 만나면 *"이건 나중에 닫아야지"* 하고 마음에 쌓아둔다. 닫는 괄호를 만나면 *가장 최근에 쌓아둔 것*과 짝이 맞는지 본다. `"({)}"`: `(` 쌓고 → `{` 쌓고 → `)` 만남, 최근은 `{` → 짝 안 맞음 → False.

**3) 암묵지 명시화:** "가장 최근에 쌓아둔 것부터 꺼내 짝을 본다." → 이게 **후입선출(LIFO)**. "짝" = `()`,`{}`,`[]` 의 대응 관계(딕셔너리로).

**4) 상태 → 변수:** 머릿속에 *쌓아둔 여는 괄호들*. 매핑표 "짝을 맞춘다 / 최근 것부터 되돌린다" → **스택**.

**5) 흐름 + 종료:** 문자 하나씩(`for`). 여는 괄호면 push, 닫는 괄호면 (스택이 비었거나 짝이 안 맞으면 즉시 False) pop. 끝났을 때 스택이 비어 있어야 True.

**6) 코드 (Python):**
```python
def is_valid(s):
    pairs = {')': '(', '}': '{', ']': '['}   # 닫는 → 여는
    stack = []
    for ch in s:
        if ch in '({[':
            stack.append(ch)                  # "나중에 닫을 것" 쌓기
        else:
            if not stack or stack.pop() != pairs[ch]:  # 비었거나 짝 불일치
                return False
    return not stack                          # 남은 게 없어야 올바름
```
- **Java/C 차이:** Java는 `Deque<Character> stack = new ArrayDeque<>();` + `push/pop/isEmpty`. C는 배열 + `top` 인덱스로 스택을 직접 구현(`stack[top++] = ch;` / `stack[--top]`). 다리(LIFO 짝 맞추기)는 동일.

**트레이싱** `"({)}"`: push`(` → push`{` → `)`:pop`{`≠`(` → **False**. 의도대로.

---

## 예제 2: 두 수의 합 → 해시(딕셔너리)

**문제:** 정수 배열에서 더해서 `target`이 되는 두 원소의 인덱스를 찾아라. 예: `[2,7,11,15]`, target=9 → `[0,1]`.

**1) 말 + 예제:** "짝이 되는 두 수를 찾아라." 작은 예제로 손풀이.

**2) 손풀이 관찰 (그리고 개선):** 처음엔 *모든 쌍*을 본다(2,7? 2,11? ...) — 느리다(O(n²)). 더 영리한 사람은: 7을 볼 때 *"나는 9가 필요해. 그럼 2가 어디 있었나?"* 를 떠올린다. 즉 **이미 본 수들을 기억**해 두고, 새 수마다 "필요한 짝을 본 적 있나?"를 묻는다.

**3) 암묵지 명시화:** "필요한 짝" = `target - 현재수`. "본 적 있나?" = 빠른 조회(상수 시간).

**4) 상태 → 변수:** *지금까지 본 수와 그 인덱스*. 매핑표 "본 적 있나? + 이름표로 찾기" → **딕셔너리** `{값: 인덱스}`.

**5) 흐름 + 종료:** 하나씩(`for`, 인덱스 포함). 매 수마다 ① 필요한 짝이 딕셔너리에 있으면 즉시 답 반환, ② 없으면 현재 수를 딕셔너리에 기록.

**6) 코드 (Python):**
```python
def two_sum(nums, target):
    seen = {}                       # 값 → 인덱스 (이미 본 수)
    for i, x in enumerate(nums):
        need = target - x           # 필요한 짝
        if need in seen:            # "본 적 있나?"  → O(1)
            return [seen[need], i]
        seen[x] = i                 # 못 찾았으면 나를 기록
    return []
```
- **Java/C 차이:** Java는 `HashMap<Integer,Integer>` + `containsKey/get/put`. C는 기본 해시맵이 없어 직접 구현하거나(혹은 정렬 + 투 포인터로 우회). 다리("이미 본 것을 기억해 한 번에 조회")는 동일.
- **핵심 도약:** 손풀이의 "모든 쌍 보기(O(n²))"를 "본 것을 기억(O(n))"으로 바꾼 게 진짜 번역. 자료구조 선택이 곧 시간복잡도다.

---

## 예제 3: 최대 연속 부분합 → 누적 변수 (Kadane)

**문제:** 정수 배열에서 *연속된* 구간의 합이 최대가 되는 값을 구하라. 예: `[-2,1,-3,4,-1,2,1,-5,4]` → `6` (4,-1,2,1).

**1) 말 + 예제:** "한 줄로 이어진 어떤 구간의 합이 가장 클까."

**2) 손풀이 관찰:** 왼쪽부터 합을 더해 나가다가, *합이 음수로 떨어지면* 그걸 끌고 가봐야 손해다 → 거기서 끊고 새로 시작한다. 가는 내내 *지금까지 본 최댓값*을 따로 적어둔다.

**3) 암묵지 명시화:** "끊고 새로 시작" = `현재합 + x` 보다 `x` 자체가 크면 `x`로 리셋. "따로 적어둔다" = 매 칸에서 최댓값 갱신.

**4) 상태 → 변수:** 두 가지를 기억함 — *여기서 끝나는 구간의 최선 합*(`cur`)과 *전체 최선 합*(`best`). 매핑표 "지금까지 중 제일 ~" → **누적 변수 2개**.

**5) 흐름 + 종료:** 하나씩(`for`). 매 칸: `cur = max(x, cur + x)` → `best = max(best, cur)`. 끝나면 `best`.

**6) 코드 (Python):**
```python
def max_subarray(nums):
    cur = best = nums[0]            # 첫 칸으로 초기화 (빈 배열 가정 안 함)
    for x in nums[1:]:
        cur = max(x, cur + x)      # 끌고 갈까 / 여기서 새로 시작할까
        best = max(best, cur)      # 지금까지 최선 갱신
    return best
```
- **Java/C 차이:** `max`를 삼항/`Math.max`로. C는 `cur = (x > cur + x) ? x : cur + x;`. 누적 변수 패턴 자체는 동일.
- **번역의 교훈:** "끊고 새로 시작" 같은 사람의 직관적 결정이, 코드에선 `max(x, cur+x)` 한 줄의 *비교*가 된다. 직관의 "판단"을 기계의 "비교 연산"으로 옮기는 게 3단계의 핵심.

---

## 패턴 요약

| 손풀이에서 이런 행동이 보이면 | 이 자료구조/패턴으로 번역 |
|---|---|
| 최근 것부터 되돌리며 짝 맞추기 | 스택 |
| "이미 본 것 있나?" 빠른 조회 | 집합 / 딕셔너리 |
| 줄 세워 들어온 순서대로 처리 | 큐 (BFS 등) |
| 양 끝에서 좁혀오기 | 투 포인터 |
| "지금까지 중 최선"을 끌고 가기 | 누적 변수 |
| 표 칸을 이전 칸들로 채우기 | DP 테이블 |

공통 교훈: **손풀이에서 "내가 뭘 기억하고, 무슨 판단을 하는가"를 관찰하면, 자료구조와 제어 흐름이 거기서 곧장 나온다.**
